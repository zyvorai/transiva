# Grafana dashboard provisioning for HyperSDK

apiVersion: 1

providers:
  - name: 'HyperSDK Dashboards'
    orgId: 1
    folder: 'HyperSDK'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
